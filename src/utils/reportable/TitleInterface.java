package fundb.utils.reportable;

// Allow objects to express meaning as title (used within `repl.reporter` package).
public interface TitleInterface {

    // Returns the title as `String`.
    public String getTitleString();
}
