package fundb.utils.result;

// Same context for `AbstractExecutionResult`. It is just imported at `fundb.database` package to
// guarantee that all class results actually extends it.
public abstract class AbstractDatabaseExecutionResult extends AbstractExecutionResult {

}
